package com.rodolfolcrm.algorithms.string;

public class Anagrams {

    /**
     * Given two strings (lowercase a->z), how many letters do we need to remove (from either) to make them anagrams?
     *
     * anagrams: some letters, same counts, different orders
     *
     * @param first
     * @param second
     * @return
     */
    public int numberNeeded(String first, String second) {
        return -1;
    }
}
